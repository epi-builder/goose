# Quick Task 260407-wa5 Plan

## Goal

Refocus the core GSD planning artifacts so the roadmap is feature-centric while preserving the successful import artifacts already present in `.planning/codebase/` and `.planning/research/`.

## Tasks

1. Update `PROJECT.md` to shift active priorities from meta/infrastructure concerns to the requested feature order.
2. Update `REQUIREMENTS.md`, `ROADMAP.md`, and `STATE.md` so phases and traceability align with the feature roadmap.
3. Preserve existing import artifacts untouched and record the change in quick-task history.
