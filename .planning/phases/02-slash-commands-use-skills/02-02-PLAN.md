---
phase: 02-slash-commands-use-skills
plan: "02"
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - ui/desktop/src/components/MentionPopover.tsx
  - ui/desktop/src/components/ChatInput.tsx
  - ui/desktop/src/components/ItemIcon.tsx
  - ui/desktop/src/components/__tests__/MentionPopover.test.tsx
  - ui/desktop/src/components/__tests__/ChatInput.test.tsx
autonomous: true
requirements:
  - SKILL-01
  - SKILL-02
  - SKILL-03
  - SKILL-04
user_setup: []
must_haves:
  truths:
    - Selecting a skill from slash suggestions inserts canonical `[name](path)` text instead of a prose stub.
    - The desktop composer renders markdown-link references, with skill links visually emphasized, while preserving canonical text round-trip.
    - UI behavior remains understandable and does not create a hidden metadata-only execution path for skills.
  artifacts:
    - ui/desktop/src/components/MentionPopover.tsx
    - ui/desktop/src/components/ChatInput.tsx
  key_links:
    - Desktop slash suggestion selection consumes the generated slash-command API contract from Plan 01.
    - Composer rendering maps back to canonical markdown-link text sent to the agent.
---

<objective>
Implement the desktop selection and composer behavior that turns discovered skills into first-class canonical references.

Purpose: Deliver the user-visible Phase 2 behavior by replacing prose insertion with structured skill references and bounded markdown-link rendering in the composer.
Output: Desktop slash selection inserts `[name](path)` for skills, generic markdown links render safely, and skill references remain visually distinct and traceable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-slash-commands-use-skills/2-CONTEXT.md
@.planning/phases/02-slash-commands-use-skills/02-RESEARCH.md
@.planning/phases/02-slash-commands-use-skills/02-VALIDATION.md
@.planning/phases/02-slash-commands-use-skills/02-01-PLAN.md
@ui/desktop/src/components/MentionPopover.tsx
@ui/desktop/src/components/ChatInput.tsx
@ui/desktop/src/components/ItemIcon.tsx
@ui/desktop/src/api/types.gen.ts
</context>

<threat_model>
## Trust Boundaries
- Generated slash-command metadata ↔ desktop selection text inserted into the composer
- Rich UI rendering ↔ underlying canonical plain-text message body sent to the agent

## STRIDE Register
| threat_id | category | component | disposition | mitigation_plan |
| --- | --- | --- | --- | --- |
| T-02-05 | Tampering | Skill insertion text | mitigate | Build selection text directly from canonical API metadata as `[name](path)`; remove the prose `Use the X skill to` path for skills. |
| T-02-06 | Spoofing | Skill vs generic markdown-link rendering | mitigate | Keep skill items explicitly typed and visually distinct from generic links while using the same underlying markdown-link parser/renderer. |
| T-02-07 | Repudiation | Sent message content | mitigate | Preserve a plain-text source of truth in `ChatInput` so logs, messages, and tests can prove the exact outgoing text remains canonical markdown-link syntax. |
| T-02-08 | Denial of Service | Composer architecture scope | mitigate | Avoid replacing the entire editor; implement the smallest rendering/token layer that preserves editing stability and bounded complexity. |
</threat_model>

<tasks>
<task type="auto">
  <name>Task 1: Replace skill prose insertion with canonical markdown-link references</name>
  <files>ui/desktop/src/components/MentionPopover.tsx, ui/desktop/src/components/ChatInput.tsx</files>
  <read_first>ui/desktop/src/components/MentionPopover.tsx, ui/desktop/src/components/ChatInput.tsx, ui/desktop/src/api/types.gen.ts, .planning/phases/02-slash-commands-use-skills/2-CONTEXT.md</read_first>
  <action>Update `ui/desktop/src/components/MentionPopover.tsx` so `getSelectionText()` no longer returns `Use the ${item.name} skill to ` for `Skill` items. Instead, use the canonical skill metadata produced by Plan 01 to insert the exact markdown-link form `[${item.name}](${item.referencePath})` or the equivalent generated field name from the API contract. Keep builtin and recipe selection behavior as `/${item.name}`. In `ChatInput.tsx`, ensure mention selection inserts that exact text into the underlying composer value without converting it to an opaque metadata object.</action>
  <verify>rg -n "Use the .* skill to|\[\$\{item.name\}\]|referencePath|onSelect\(" ui/desktop/src/components/MentionPopover.tsx ui/desktop/src/components/ChatInput.tsx</verify>
  <acceptance_criteria>
    - `MentionPopover.tsx` no longer contains the string pattern `Use the ${item.name} skill to`
    - Skill selection text is constructed as markdown-link text using the selected skill name and canonical path/reference field
    - Builtin and recipe items still insert `/${item.name}`
    - `ChatInput.tsx` continues to receive inserted plain text rather than a separate hidden skill metadata channel
  </acceptance_criteria>
  <done>Desktop slash selection inserts canonical skill references instead of prose.</done>
</task>

<task type="auto">
  <name>Task 2: Add bounded markdown-link rendering with skill-emphasized presentation</name>
  <files>ui/desktop/src/components/ChatInput.tsx, ui/desktop/src/components/ItemIcon.tsx</files>
  <read_first>ui/desktop/src/components/ChatInput.tsx, ui/desktop/src/components/ItemIcon.tsx, .planning/phases/02-slash-commands-use-skills/2-CONTEXT.md, .planning/phases/02-slash-commands-use-skills/02-RESEARCH.md</read_first>
  <action>Implement the smallest viable rendering/parsing layer in `ChatInput.tsx` that recognizes arbitrary `[name](path)` markdown-link sequences in the composer, renders them with richer inline UI, and preserves the underlying source text for editing and send behavior. Render skill references with a visually emphasized chip/tag treatment and path-aware styling, while generic markdown links remain supported but visually less specialized. If `ItemIcon.tsx` or related shared UI helpers are used in the popover or token rendering, update them so `Skill` items remain clearly distinguishable from `Recipe` and `Builtin` items.</action>
  <verify>rg -n "\[name\]\(path\)|markdown|link|Skill|chip|token|render" ui/desktop/src/components/ChatInput.tsx ui/desktop/src/components/ItemIcon.tsx</verify>
  <acceptance_criteria>
    - `ChatInput.tsx` contains logic that recognizes markdown-link-shaped text rather than only skill-specific prose
    - The implementation supports arbitrary `[name](path)` links, not only skills
    - Skill links receive a visibly distinct rendering path from generic links
    - The underlying sent or stored message text remains canonical markdown-link text
  </acceptance_criteria>
  <done>The desktop composer renders markdown-link references without losing canonical text round-trip.</done>
</task>

<task type="auto">
  <name>Task 3: Add desktop tests for selection, rendering, and round-trip behavior</name>
  <files>ui/desktop/src/components/__tests__/MentionPopover.test.tsx, ui/desktop/src/components/__tests__/ChatInput.test.tsx</files>
  <read_first>ui/desktop/src/components/MentionPopover.tsx, ui/desktop/src/components/ChatInput.tsx, ui/desktop/package.json, .planning/phases/02-slash-commands-use-skills/02-VALIDATION.md</read_first>
  <action>Create or update desktop tests that prove three concrete behaviors: (1) selecting a `Skill` item in `MentionPopover` inserts `[name](path)` text, (2) selecting a `Builtin` or `Recipe` item still inserts `/name`, and (3) `ChatInput` renders skill links distinctly while preserving the actual text value as `[name](path)` after insertion, editing, and send-trigger preparation. Use the existing desktop test stack from `ui/desktop/package.json` and `vitest.config.ts` rather than adding a new test framework.</action>
  <verify>test -f ui/desktop/src/components/__tests__/MentionPopover.test.tsx && test -f ui/desktop/src/components/__tests__/ChatInput.test.tsx && rg -n "\[.*\]\(.*\)|Skill|Recipe|Builtin|/name|onSelect|send" ui/desktop/src/components/__tests__/MentionPopover.test.tsx ui/desktop/src/components/__tests__/ChatInput.test.tsx</verify>
  <acceptance_criteria>
    - `MentionPopover.test.tsx` exists and asserts skill insertion uses markdown-link text
    - `MentionPopover.test.tsx` asserts builtin or recipe insertion still uses slash-prefixed command text
    - `ChatInput.test.tsx` exists and asserts rendered skill references preserve the underlying text value `[name](path)`
    - Tests use the existing desktop Vitest setup
  </acceptance_criteria>
  <done>Desktop behavior is covered by regression tests for selection and canonical text round-trip.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd ui/desktop && pnpm test -- --run MentionPopover ChatInput`
- [ ] `rg -n "Use the .* skill to" ui/desktop/src/components/MentionPopover.tsx` returns no matches
- [ ] `rg -n "\[.*\]\(.*\)" ui/desktop/src/components/MentionPopover.tsx ui/desktop/src/components/ChatInput.tsx ui/desktop/src/components/__tests__/MentionPopover.test.tsx ui/desktop/src/components/__tests__/ChatInput.test.tsx`
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Desktop slash skill selection and rendering are canonical, understandable, and stable
</success_criteria>

<output>
After completion, create `.planning/phases/02-slash-commands-use-skills/02-02-SUMMARY.md`
</output>
