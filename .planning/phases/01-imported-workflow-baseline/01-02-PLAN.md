---
phase: 01-imported-workflow-baseline
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements:
  - BASE-02
  - BASE-03
user_setup: []
must_haves:
  truths:
    - Core planning docs remain aligned to the feature-centric roadmap without erasing imported brownfield context.
    - Contributors can see, from top-level planning docs, how the imported baseline connects to later feature phases.
  artifacts:
    - .planning/PROJECT.md
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - .planning/STATE.md
  key_links:
    - The top-level planning docs refer consistently to the imported baseline and Phase 1 preservation contract.
---

<objective>
Align the top-level planning documents so they consistently point back to the preserved imported baseline and explain its role in the feature roadmap.

Purpose: Ensure the project-level planning contract stays feature-centric while preserving brownfield import context instead of letting later edits drift away from it.
Output: Focused updates to `.planning/PROJECT.md`, `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, and `.planning/STATE.md` that reinforce the Phase 1 baseline contract.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-imported-workflow-baseline/1-CONTEXT.md
@.planning/phases/01-imported-workflow-baseline/01-ARTIFACT-INVENTORY.md
</context>

<threat_model>
## Trust Boundaries
- Top-level planning docs ↔ contributors using them as project-wide source of truth
- Existing roadmap/requirements language ↔ new wording that clarifies preservation without changing scope

## STRIDE Register
| threat_id | category | component | disposition | mitigation_plan |
| --- | --- | --- | --- | --- |
| T-01-04 | Tampering | Top-level planning docs | mitigate | Limit edits to wording that clarifies baseline preservation and downstream usage; do not add new product requirements or reorder phases. |
| T-01-05 | Repudiation | Baseline-to-phase linkage | mitigate | Add explicit traceable language in project-level docs that references Phase 1's preservation role and downstream planning expectations. |
| T-01-06 | Denial of Service | Planning usability | mitigate | Keep edits concise and additive so contributors can still read the top-level docs quickly without duplicating the full inventory. |
</threat_model>

<tasks>
<task type="auto">
  <name>Task 1: Update project-level baseline narrative</name>
  <files>.planning/PROJECT.md, .planning/ROADMAP.md</files>
  <read_first>.planning/PROJECT.md, .planning/ROADMAP.md, .planning/phases/01-imported-workflow-baseline/1-CONTEXT.md, .planning/phases/01-imported-workflow-baseline/01-ARTIFACT-INVENTORY.md</read_first>
  <action>Revise `.planning/PROJECT.md` and `.planning/ROADMAP.md` only where needed so they explicitly state that the canonical baseline includes the four core planning docs plus `.planning/codebase/` and `.planning/research/`, and that Phase 1 operationalizes that baseline for later feature phases. Preserve the existing feature order `Phase 1 → Phase 2 → Phase 3 → Phase 4 → Phase 5`. Do not add new phases, requirements, or capabilities. In `ROADMAP.md`, ensure the Phase 1 narrative makes contributors aware that later phases inherit this baseline and must document divergence when they depart from it.</action>
  <verify>rg -n "canonical baseline|\.planning/codebase/|\.planning/research/|divergence|later phases" .planning/PROJECT.md .planning/ROADMAP.md</verify>
  <acceptance_criteria>
    - `.planning/PROJECT.md` contains the phrase `canonical baseline`
    - `.planning/PROJECT.md` mentions both `.planning/codebase/` and `.planning/research/`
    - `.planning/ROADMAP.md` mentions `later phases` or equivalent downstream inheritance language
    - `.planning/ROADMAP.md` still lists Phase 1 through Phase 5 in the same order
  </acceptance_criteria>
  <done>The project-level narrative explicitly preserves the imported baseline without changing roadmap scope.</done>
</task>

<task type="auto">
  <name>Task 2: Tighten requirements and state traceability</name>
  <files>.planning/REQUIREMENTS.md, .planning/STATE.md</files>
  <read_first>.planning/REQUIREMENTS.md, .planning/STATE.md, .planning/phases/01-imported-workflow-baseline/1-CONTEXT.md, .planning/phases/01-imported-workflow-baseline/01-ARTIFACT-INVENTORY.md</read_first>
  <action>Update `.planning/REQUIREMENTS.md` and `.planning/STATE.md` so Phase 1 traceability reflects the preservation contract more explicitly. In `REQUIREMENTS.md`, keep `BASE-01`, `BASE-02`, and `BASE-03` intact but clarify that canonical inputs are identifiable and preserved for downstream planning. In `STATE.md`, add or refine Phase 1 status language so the current focus notes that imported artifacts remain canonical inputs and that planning readiness depends on explicit baseline documentation, not replacement documentation.</action>
  <verify>rg -n "canonical inputs|downstream planning|planning readiness|replacement documentation" .planning/REQUIREMENTS.md .planning/STATE.md</verify>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` contains the phrase `canonical inputs`
    - `.planning/STATE.md` contains the phrase `planning readiness`
    - `.planning/STATE.md` contains the phrase `replacement documentation` or equivalent anti-replacement wording
  </acceptance_criteria>
  <done>The requirements and state docs reinforce the same baseline-preservation contract as the project and roadmap docs.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "canonical baseline|\.planning/codebase/|\.planning/research/|divergence|later phases" .planning/PROJECT.md .planning/ROADMAP.md`
- [ ] `rg -n "canonical inputs|downstream planning|planning readiness|replacement documentation" .planning/REQUIREMENTS.md .planning/STATE.md`
- [ ] `git diff -- .planning/PROJECT.md .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/STATE.md`
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The four top-level planning docs consistently reinforce the preserved imported baseline and its connection to future phases
</success_criteria>

<output>
After completion, create `.planning/phases/01-imported-workflow-baseline/01-02-SUMMARY.md`
</output>
